# Copy example.docksal-local.yml to docksal-local.yml.

# After editing, apply changes with 'fin up'

version: "3.9"

# services:

  # solr:
  #   extends:
  #     file: ${HOME}/.docksal/stacks/services.yml
  #     service: solr
  #   image: ${SOLR_IMAGE:-docksal/solr:7.7-2.1}
  #   volumes:
  #     - ${PROJECT_ROOT}/.docksal/etc/solr/${SOLR_CORE:-acquia_copy}:/opt/solr/server/solr/configsets/${SOLR_CORE:-acquia_copy}:rw

  # mail:
  #   extends:
  #     file: ${HOME}/.docksal/stacks/services.yml
  #     service: mail
