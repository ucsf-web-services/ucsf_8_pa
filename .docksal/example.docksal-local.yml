# Copy example.docksal-local.yml to docksal-local.yml.
# Do not change docksal.yml.
# Uncomment or add what you need to docksal-local.yml.

# After editing, apply changes with 'fin up'

version: "3.9"

# services:

  # mail:
  #   extends:
  #     file: ${HOME}/.docksal/stacks/services.yml
  #     service: mail

  # solr:
  #   extends:
  #     file: ${HOME}/.docksal/stacks/services.yml
  #     service: solr
  #   image: ${SOLR_IMAGE:-docksal/solr:7.7-2.1}