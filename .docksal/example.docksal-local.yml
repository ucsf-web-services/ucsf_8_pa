# Copy example.docksal-local.yml to docksal-local.yml.

version: "3.9"

# services:

  # Solr
  # solr:
  #   extends:
  #     file: ${HOME}/.docksal/stacks/services.yml
  #     service: solr
  #   volumes:
  #     - ${PROJECT_ROOT}/.docksal/etc/solr/conf:/var/lib/solr/conf:ro

  # MailHog
  # mail:
  #   extends:
  #     file: ${HOME}/.docksal/stacks/services.yml
  #     service: mail

  # PHPMyAdmin
  # pma:
  #   hostname: pma
  #   image: phpmyadmin/phpmyadmin
  #   environment:
  #     - PMA_HOST=db
  #     - PMA_USER=root
  #     - PMA_PASSWORD=${MYSQL_ROOT_PASSWORD:-root}
  #     - MEMORY_LIMIT=1G
  #     - UPLOAD_LIMIT=1G
  #   labels:
  #     - io.docksal.virtual-host=pma.${VIRTUAL_HOST}