{% set classes = ['blockquote', 'blockquote-full-width'] %}

<blockquote{{ attributes.addClass(classes) }}>
  <div class="blockquote-content">
    {{ content|without('field_blockquote_image') }}
  </div>
  {{ content.field_blockquote_image }}
</blockquote>
